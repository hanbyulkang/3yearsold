# Team Agent 공동규칙

## 1. 목적

이 Vault는 Local AI Boost Club 해커톤에서 팀원과 각자의 로컬 에이전트가 같은 사실, 작업 상태, 결정사항, 인수인계 내용을 공유하기 위한 협업 공간입니다.

이 문서는 이 Vault를 읽고 수정하는 모든 팀 에이전트의 공통 운영 기준입니다.

핵심 목표는 에이전트끼리 멋지게 대화하는 것이 아니라, **팀원이 언제든 현재 상태와 다음 행동을 확인할 수 있게 하는 것**입니다.

---

## 2. 기준본과 파일 역할

모든 에이전트는 작업을 시작하기 전에 다음 파일을 확인합니다.

1. `00-dashboard/current-status.md` — 현재 목표와 전체 상태
2. `03-tasks/task-board.md` — 담당 작업과 중복 여부
3. `02-decisions/decision-log.md` — 이미 확정된 결정
4. 자신의 이전 인수인계 파일 — 이어서 해야 할 작업

각 폴더의 역할은 다음과 같습니다.

- `00-dashboard/`: 현재 상태와 다음 우선순위
- `01-brief/`: 문제·사용자·제약·평가 기준
- `02-decisions/`: 팀의 결정과 결정 근거
- `03-tasks/`: 작업 목록·담당자·상태·막힘
- `04-research/`: 조사 결과와 출처
- `05-data/`: 데이터 구조·샘플·품질 상태
- `06-handoffs/`: 에이전트 세션 인수인계
- `07-demo/`: 데모 흐름·발표·시연 준비
- `08-evidence/`: 실행 결과·스크린샷·검증 근거의 색인

파일이 서로 충돌하면 최신 파일이 무조건 이기는 것이 아니라, **실제 실행 결과와 검증 근거가 있는 기록**을 우선합니다.

---

## 3. 팀 운영 철학

### 3.1 주제를 미리 고정하지 않는다

당일 주제가 공개되는 해커톤에서는 특정 아이디어를 미리 완성하려 하지 않습니다.

미리 준비할 것은 다음입니다.

- 문제 정의 방식
- 사용자·상황·손실을 정리하는 템플릿
- 데이터베이스 구조
- AI 도구 사용법
- 데모 제작 방식
- 검증·인수인계 방법

주제가 나오면 다음 순서로 빠르게 좁힙니다.

```text
누가 → 어떤 상황에서 → 무엇을 겪고 → 왜 기존 방법이 부족하며 → AI가 어떤 판단을 더하는가
```

### 3.2 기록보다 행동이 우선이다

문서를 많이 만드는 것이 목표가 아닙니다.

모든 기록은 다음 행동으로 이어져야 합니다.

- 누가 할 것인가?
- 무엇을 만들 것인가?
- 언제까지 할 것인가?
- 완료를 무엇으로 증명할 것인가?

### 3.3 사실·해석·제안을 분리한다

다음 세 가지를 섞지 않습니다.

- `사실`: 실제 파일·로그·원문·실행 결과로 확인된 내용
- `해석`: 사실을 바탕으로 한 분석
- `제안`: 아직 선택되지 않은 다음 방향

확인하지 못한 내용은 `미확인`, `가설`, `검증 필요`로 표시합니다.

### 3.4 완료는 증거가 있을 때만 선언한다

다음은 완료 증거가 될 수 있습니다.

- 실행 가능한 파일
- 실제 실행 로그
- 테스트 결과
- 검증된 URL
- 입력·출력 샘플
- 발표자가 다시 재현할 수 있는 데모 절차

코드를 작성했다는 사실만으로 기능이 완료된 것은 아닙니다.

---

## 4. 작업 시작 규칙

에이전트는 작업을 시작하기 전에 다음을 수행합니다.

1. `current-status.md`를 읽는다.
2. `decision-log.md`를 읽어 이미 정해진 방향을 확인한다.
3. `task-board.md`에서 같은 작업을 다른 사람이 맡고 있는지 확인한다.
4. 작업을 시작하기 전에 본인의 작업을 `in_progress`로 등록한다.
5. 입력 자료와 기대 출력물을 명시한다.

작업을 등록하지 않고 시작하면 팀이 중복 작업을 막을 수 없습니다.

### 작업 등록 형식

```markdown
- [ ] 작업 제목
  - id: unique-task-id
  - owner: 팀원 또는 에이전트명
  - status: in_progress
  - input: 사용할 자료
  - output: 만들 결과물
  - next_action: 지금 할 일
  - evidence: 완료 후 남길 근거
```

---

## 5. 작업 상태 규칙

상태는 다음 중 하나를 사용합니다.

- `idea`: 아이디어 단계
- `exploring`: 조사·탐색 중
- `selected`: 팀이 방향을 선택함
- `in_progress`: 실행 중
- `blocked`: 외부 정보·권한·기술 문제로 막힘
- `review`: 다른 팀원의 검토 필요
- `verified`: 실행·검증 완료
- `done`: 결과물이 팀 기준본에 반영됨
- `dropped`: 사용하지 않기로 결정함

`done`은 단순히 작업자가 끝났다고 느끼는 상태가 아닙니다.

- 결과물이 존재하고
- 실행 또는 검토가 끝났고
- 근거가 기록되어 있고
- 다른 팀원이 이어받을 수 있어야 합니다.

---

## 6. 결정 관리

팀의 방향·기술·데이터·발표 구조를 결정하면 `02-decisions/decision-log.md`에 남깁니다.

```markdown
## D-001 — 결정 제목

- date:
- decision:
- alternatives:
- reason:
- evidence:
- tradeoff:
- owner:
- status: active / superseded
```

이미 `active`인 결정과 충돌하는 작업을 시작하려면 먼저 결정 로그를 확인합니다.

결정이 바뀌면 기존 기록을 지우지 않습니다. 새 결정을 추가하고 이전 결정은 `superseded`로 표시합니다.

---

## 7. 조사와 출처 규칙

조사 결과에는 반드시 출처를 남깁니다.

```markdown
## 출처 항목

- title:
- url_or_path:
- accessed_at:
- source_type: official / research / community / sample / internal
- claim:
- confidence: high / medium / low
- used_for:
```

검색 결과의 제목이나 요약만 보고 확정하지 않습니다. 중요한 주장은 원문·파일·실행 결과를 확인합니다.

출처가 없는 수치·사용자 반응·시장 규모·성능 주장은 발표자료에 확정 사실로 넣지 않습니다.

---

## 8. 데이터와 개인정보

이 Vault에는 해커톤에 필요한 최소 데이터만 둡니다.

저장하지 않는 것:

- 비밀번호
- API 키와 토큰
- 쿠키·세션 파일
- 개인 계정 인증 정보
- 팀원의 불필요한 개인정보
- 원본 개인정보가 포함된 실제 사용자 데이터

필요한 경우 개인정보를 제거한 샘플 데이터를 사용합니다.

데이터를 기록할 때는 다음을 구분합니다.

- 원본 입력
- 정제된 입력
- 라벨 또는 기대 판단
- 출처
- 개인정보 제거 여부
- 품질 문제
- 사용 허가 또는 공개 범위

---

## 9. 에이전트 인수인계 규칙

세션이 끝나거나 다른 에이전트에게 작업을 넘길 때 `06-handoffs/`에 인수인계를 남깁니다.

```markdown
# Agent Handoff

- agent:
- human_owner:
- updated_at:
- task:
- status: in_progress / verified / blocked / done

## 이번 세션에서 확인한 사실
-

## 실제로 실행한 작업
-

## 생성·수정한 파일
-

## 검증 결과
-

## 아직 확정하지 못한 것
-

## 다음 에이전트가 바로 할 일
1.
2.
3.

## 주의할 점
-
```

인수인계에는 “열심히 했다” 같은 감상보다 다음 에이전트가 바로 행동할 수 있는 정보가 필요합니다.

특히 반드시 구분합니다.

- 논의만 한 것
- 실제 실행한 것
- 검증된 것
- 아직 막힌 것

---

## 10. 충돌과 동시 수정

같은 파일을 여러 명이 동시에 수정하면 충돌이 생길 수 있습니다.

우선순위:

1. 결정 로그는 한 번에 한 명이 갱신한다.
2. 작업 보드는 짧게 수정하고 즉시 저장한다.
3. 대형 문서는 담당자를 정한다.
4. 다른 사람이 작업 중인 파일을 통째로 덮어쓰지 않는다.
5. 충돌이 생기면 임의로 합치지 말고 `blocked`로 표시한 뒤 팀에 알린다.
6. 기존 내용이 사라지는 방식으로 병합하지 않는다.

Git을 사용한다면 작업 단위별로 작은 커밋을 만들고, 커밋 전에 다음을 확인합니다.

- 변경 파일
- 기존 작업과의 충돌
- 민감정보 포함 여부
- 실행·검증 결과

---

## 11. 보고 형식

팀 에이전트의 보고는 다음 순서로 작성합니다.

1. 결론
2. 실제로 한 일
3. 근거와 검증 결과
4. 아직 모르는 것
5. 다음 행동

예시:

```text
상태: verified
결론: 음성 입력에서 의도 분류까지 정상 동작합니다.
실행: sample_01.wav와 sample_02.wav로 테스트했습니다.
근거: evidence/run-2026-07-24.md
미확인: 실제 사용자 환경의 소음 조건은 아직 검증하지 않았습니다.
다음 행동: 발표용 오류 사례 1개를 추가합니다.
```

“완료했습니다”라고 말할 때는 파일이나 실행 근거를 함께 적습니다.

---

## 12. 해커톤 우선순위

시간이 부족할 때 우선순위는 다음과 같습니다.

1. 사용자가 누구인지와 문제가 무엇인지 명확히 한다.
2. AI가 필요한 판단을 한 문장으로 정의한다.
3. 발표장에서 재현 가능한 30초 데모를 만든다.
4. 입력·처리·출력의 데이터 흐름을 안정화한다.
5. 실패 사례와 한계를 하나 이상 준비한다.
6. 발표 자료와 실제 구현의 표현을 일치시킨다.
7. 부가 기능은 핵심 데모가 검증된 뒤 추가한다.

예쁜 문서, 복잡한 아키텍처, 많은 기능보다 **명확한 문제·실행되는 데모·검증 가능한 근거**가 우선입니다.

---

## 13. 금지되는 운영 방식

- 확인하지 않은 내용을 사실처럼 기록하기
- 출처 없는 수치를 발표자료에 넣기
- 다른 사람이 맡은 작업을 등록 없이 중복 수행하기
- 작업 중인 파일을 통째로 덮어쓰기
- 코드가 있다는 이유로 실행 완료라고 보고하기
- 개인 키·토큰·비밀번호를 Vault에 저장하기
- 결정이 바뀌었는데 기존 결정을 삭제하기
- 세션이 끝났는데 인수인계를 남기지 않기
- 팀원이 읽지 않는 복잡한 문서를 만드는 데 시간을 쓰기

---

## 14. 최종 원칙

이 팀 Vault의 운영 원칙은 다음 다섯 문장으로 압축됩니다.

1. **같은 사실을 보고 작업한다.**
2. **작업을 시작하기 전에 담당과 결과물을 등록한다.**
3. **결정은 근거와 함께 남긴다.**
4. **완료는 실행 증거가 있을 때만 선언한다.**
5. **다음 사람이 바로 이어갈 수 있게 인수인계한다.**
