# PRD — D+ (디플러스)

**장기 미입양 유기견 리워드형 타이쿤 게임**
CODEGATE 2026 · AI 스타트업 해커톤 본선

| 항목 | 내용 |
|---|---|
| 문서 버전 | v0.9 (2026-07-21) |
| 상태 | 팀 리뷰 대기 |
| 대상 독자 | 기획자 · 백엔드 개발자 · Unity 개발자 |
| 플랫폼 | **Unity WebGL** (웹 브라우저에서 플레이, 모바일 세로 393×852 기준) |
| 스택 | Unity 6 (WebGL) + Supabase (Auth·DB·RLS·Edge Functions) + Claude API |
| 디자인 | Figma — [D+ 타이쿤 파일](https://www.figma.com/design/NG6doeqfscOobq2Ilt5mPA) · 아이소메트릭 도트 |

---

## 1. 한 줄 정의

> 철창 앞 사진 한 장에서 멈춘 장기 미입양견을, AI가 매일 이야기를 만드는 캐릭터로 바꿔
> **돌봄(다마고치) → 재화(미니게임) → 실물 기부 → 입양 매칭**까지 잇는 웹 게임.

### 1.1 왜 이 제품인가 (문제)

- 보호소 공고는 **첫날 이후 갱신되지 않는다.** 겁먹은 사진 한 컷 + 견종·체중·색상 아홉 줄이 그 개의 전부.
- 검정·대형·노령·믹스견은 그 한 컷에서 먼저 탈락한다. 연간 유실·유기동물 약 11만 마리, 입양률 3할 미만. *(발표 전 최신 연도 수치 확정 필요)*
- 병목은 **돈이 아니라 노출과 정보**다. 사람 손으로는 개 한 마리당 콘텐츠를 계속 만들 수 없다.

### 1.2 핵심 명제

**개 1마리 = 공고문 1장 → 개 1마리 = 매일 갱신되는 콘텐츠 채널 1개.**
보호소의 일은 사진 1장 + 메모 3줄 업로드로 끝난다. 나머지(캐릭터·성격·일기·매칭·스크리닝)는 전부 AI 파이프라인.

### 1.3 설계 원칙 (전 기능 공통, 위반 시 기능 드랍)

1. **소유가 아니라 참여** — 유저는 개를 "가지지" 않는다. 법적 보호 주체는 끝까지 보호소.
2. **죽음·안락사를 게임 장치로 쓰지 않는다** — 실패 연출, 긴장 유발, 타이머 어디에도 금지.
3. **기부는 폐루프로 증명** — 집행 내역이 그 개의 다음 일기 콘텐츠로 돌아온다.
4. **입양은 부추기지 않고 거른다** — 성장치 미달이면 신청 자체가 안 열린다.

---

## 2. 목표와 지표

| 구분 | 지표 | 데모/시즌1 목표 |
|---|---|---|
| North Star | **입양 후 6개월 유지율** | 측정 체계 구축 (해커톤에선 정의만) |
| 핵심 | 등록견당 주간 노출 수 (일기 조회+공유) | 공고문 대비 10배 |
| 핵심 | 입양 신청 전환 수 / 스크리닝 통과율 | 파이프라인 동작 시연 |
| 참여 | D7 리텐션 / 일일 돌봄 완료율 | D7 20% (시즌1) |
| 기부 | 월간 실물 기부 전환량 (사료 kg) | 데모: 모의 정산 1건 완주 |

**해커톤 데모 성공 기준**: 무대에서 90초 안에 "실제 공고 사진 1장 업로드 → 캐릭터 생성 → 성격 스탯 → 플레이 → 일기 생성 → 매칭 점수 → 스크리닝 결과"가 실제로 돈다.

---

## 3. 대상 사용자

| 페르소나 | 설명 | 핵심 동기 |
|---|---|---|
| **플레이어 (주 타깃)** | 20~30대, 반려동물을 키우고 싶지만 현실 여건(원룸·직장)으로 망설이는 사람. 방치형·수집형 게임 경험 있음 | 죄책감 없는 참여, "내가 논 게 실제 도움이 됨" |
| **예비 입양자** | 플레이어 중 성장치 상위권. 입양을 진지하게 고민 | 이 개와 정말 맞는지 확인하고 싶음 |
| **보호소 직원** | 지자체 보호소·사설 쉼터. 행정 업무 과중, 홍보 여력 0 | 업로드 1회로 홍보 자동화, 위험 신청자 필터 |
| **후원 브랜드** | 사료·용품 브랜드 CSR/마케팅 담당 | 진정성 있는 노출, 집행 증빙 |

---

## 4. 제품 구조 — 3개 루프

```
        ┌──────────────── ① 돌봄 루프 (다마고치 · 메인) ────────────────┐
        │  대표견 마당(타이쿤) → 밥·산책·교감 → 성장치↑ → 마당 확장      │
        └──────────────────────────┬──────────────────────────────┘
                                   │ 하트(행동력) 소모
        ┌──────────────── ② 재화 루프 (미니게임) ──────────────────────┐
        │  3매치 플레이 → 사료 재화 획득 → 사료 창고 적립               │
        │  창고가 차면 → **실물 사료 기부** (주간 랭킹 1위 이름으로)      │
        └──────────────────────────┬──────────────────────────────┘
                                   │ 성장치 = 입양 자격
        ┌──────────────── ③ 연결 루프 (입양 · 목적) ────────────────────┐
        │  지역별 유기견 탐색 → 관심견 등록 → 플레이 데이터 기반 매칭    │
        │  → 현실 시뮬레이션 → 신청 → AI 스크리닝 → 보호소 전달         │
        └─────────────────────────────────────────────────────────┘
```

### 4.1 ① 돌봄 루프 — 다마고치/타이쿤 (메인)

홈 화면 = 대표견의 아이소메트릭 마당. (Figma 홈 화면 기준)

- **대표견**: 유저가 지역별 유기견 목록에서 1마리를 골라 "우리 마당"에 초대. 실제 보호소의 실존 개이며, 캐릭터는 AI가 공고 사진에서 생성(무늬·귀·색 보존, 원본 사진 항상 병기).
- **성격 반영**: 보호소 메모 3줄 → AI가 경계심·활동성·사람친화도 스탯화 → 게임 내 행동에 실제 반영. 낯 가리는 개는 초반에 다가가면 도망간다. *→ 사전 합사 시뮬레이터 역할*
- **돌봄 행동**: 밥 주기 / 산책 / 교감(쓰다듬기·놀아주기). 각 행동은 **하트 1개** 소모.
- **하트(행동력)**: 최대 5개, 시간 회복(30분/개). 하단 탭 [하트]에서 잔여 확인. *광고 시청 충전은 넣지 않는다(§8 BM 참조).*
- **성장치 (우리 마당 성장치)**: 산책·돌봄·교감 3개 하위 스탯의 종합 레벨. **LV.10 도달 = 입양 신청 자격 해금.** 마당 확장(타일·오브젝트)도 성장치로 열린다.
- **오늘의 일기**: 그날의 플레이 기록 + 보호소 실제 근황을 AI가 묶어 매일 1편의 일기·공유 카드 자동 생성. SNS 공유 = 노출 루프의 출구.
- **입양 성사 시**: 마당은 "임시보호 마당"으로 전환되고 다음 개를 초대. 떠난 개의 일기는 "졸업 앨범"에 보존. 이탈이 아니라 **졸업**으로 연출.

### 4.2 ② 재화 루프 — 3매치 미니게임

- **장르**: 3매치 퍼즐 (사료·간식·장난감·물·비타민 5종 타일, 8×8 보드, 이동 횟수제).
- **입장**: 하트 1개 소모. 스테이지 클리어 → **사료 재화** + 코인 획득.
- **사료 창고 (핵심 장치)**: 전 유저가 모은 사료 재화가 **전국 공동 창고**에 적립된다. 개인 창고가 아니다 — "다 같이 채우는" 구조가 기부 명분과 어뷰징 방어(개인 환금성 차단)를 동시에 해결한다.
- **실물 기부 전환**: 공동 창고가 목표치(예: 100만 사료 = 실물 1톤)에 도달하면 후원 브랜드가 실제 사료를 보호소에 기부. **기부 증서에는 그 주간 랭킹 1위 유저의 이름(닉네임)이 들어간다.** 2~10위는 증서에 연명. 집행 사진·내역은 해당 보호소 개들의 일기로 되돌아온다(폐루프).
- **랭킹**: 주간 시즌제. 점수 = 3매치 점수 + 돌봄 성실도 가중(§보안: 순수 퍼즐 점수만으로 1위가 못 되게 — 매크로 유인 축소).
- **확장(로드맵)**: 미니게임 2종 추가 슬롯 — 산책 리듬게임, 간식 타이밍게임.

### 4.3 ③ 연결 루프 — 지역별 유기견 & 입양 매칭

- **지역별 유기견 지도/목록**: 시·군·구 단위로 보호 중인 개 탐색. 데이터 소스는 **국가동물보호정보시스템 공공 API** + 제휴 보호소 직접 등록. 필터: 지역·크기·나이·기다린 날수(D+). **기본 정렬은 D+ 내림차순** — 오래 기다린 개가 먼저 보인다.
- **관심견**: 최대 3마리 팔로우. 관심견의 일기가 피드에 뜬다. 대표견 교체 가능.
- **매칭 점수**: 설문이 아니라 **플레이 행동 데이터**로 산출 — 어떤 성격의 개와 오래 상호작용했는지, 돌봄을 얼마나 꾸준히 했는지(성장치), 하트를 어디에 썼는지.
- **현실 시뮬레이션**: 입양 신청 직전, 신청자의 주거·근무 정보를 받아 "당신의 원룸 + 주 5일 9시간 근무에서 이 아이의 하루"를 AI가 서술 + 그 개의 성격 스탯 기준 마찰 지점을 명시. **부추기지 않고 거르는 단계.**
- **신청 → 스크리닝 → 전달**: §7 보안 참조. 최종 판단은 항상 보호소 사람이 한다.

---

## 5. 화면 목록 (기획자 산출물 기준)

| # | 화면 | 핵심 요소 | 우선순위 |
|---|---|---|---|
| S1 | **홈 (마당)** | 아이소메트릭 마당, 대표견, D+ 카운터, 성장치 패널, HUD(하트·코인) | P0 · Figma 완료 |
| S2 | 온보딩 | 지역 선택 → 유기견 3마리 제시(D+ 순) → 대표견 선택 | P0 |
| S3 | 3매치 플레이 | 8×8 보드, 이동 수, 사료 획득 연출 | P0 |
| S4 | 사료 창고 | 전국 공동 게이지, 이번 주 기부까지 잔여량, 지난 기부 증서 | P0 |
| S5 | 랭킹 | 주간 랭킹, 1위 = 기부 증서 명의 안내, 리워드 수령 | P0 |
| S6 | 지역별 유기견 | 지도/리스트 토글, D+ 정렬, 필터 | P1 |
| S7 | 개 프로필 | 캐릭터+원본 사진 병기, 성격 스탯, 일기 아카이브, 관심 등록 | P1 |
| S8 | 오늘의 일기 | 일기 카드, SNS 공유 | P1 |
| S9 | 매칭·시뮬레이션 | 매칭 점수, 현실 시뮬레이션 결과, 신청 버튼(자격 미달 시 잠금) | P1 |
| S10 | 뱃지 | 돌봄·기부·공유 업적 | P2 |
| S11 | 보호소 어드민(웹) | 개 등록(사진+메모 3줄), 신청자 리스크 리포트 열람 | P1 · Unity 아님, 웹 |

---

## 6. AI 기능 명세 (전부 Claude API, Edge Function 경유)

| # | 기능 | 입력 → 출력 | 비고 |
|---|---|---|---|
| A1 | 캐릭터 생성 | 공고 사진 1장 → 도트 캐릭터 파라미터(팔레트·무늬 맵·귀/꼬리 타입) | 이미지 생성이 아니라 **파라미터 추출 → 클라 스프라이트 조합**. 스타일 통일·비용·속도 모두 유리 |
| A2 | 성격 스탯화 | 보호소 메모 3줄 → 5축 스탯(경계심·활동성·사람친화·식탐·독립성) + 근거 문장 | 정답셋 20건으로 검증 (Day1 기획 태스크) |
| A3 | 오늘의 일기 | 플레이 로그 요약 + 보호소 근황 → 일기 1편 + 공유 카드 문구 | 일 1회 배치, 캐시 |
| A4 | 매칭 점수 | 플레이 행동 집계 + 개 스탯 → 궁합 점수 + 사유 3줄 | 규칙 스코어 + LLM 사유 생성 하이브리드 |
| A5 | 현실 시뮬레이션 | 신청자 환경 정보 + 개 스탯 → "이 집에서의 하루" 서술 + 마찰 지점 | 거르는 단계. 낙관 서술 금지 프롬프트 |
| A6 | 신청자 스크리닝 | 신청서 텍스트 + 계정 행동 패턴 → 위험 신호 리포트 | **판정이 아니라 리포트.** 최종 판단은 보호소 |
| A7 | 어뷰징 이상탐지 | 플레이 이벤트 시퀀스 → 봇 확률 | 서버 사이드 배치 |

---

## 7. 보안 설계 (CODEGATE 심사 대응 — 제품의 정식 기능)

### 7.1 리워드 어뷰징 방어 (재화가 실물 기부로 전환되므로 = 금전 사고 영역)

- **서버 권위 원칙**: 점수·사료·코인 계산은 전량 Edge Function에서. Unity 클라는 입력 이벤트만 보내고 결과를 표시만 한다. WebGL은 코드가 통째로 노출되는 플랫폼 — **클라이언트에 어떤 판정 로직도 두지 않는다.**
- **3매치 검증**: 클라가 "점수"를 보내지 않는다. **보드 시드 + 이동 시퀀스**를 보내면 서버가 리플레이 시뮬레이션으로 점수를 재계산. 불일치 = 무효.
- **레이트 리밋**: 하트 시스템 자체가 1차 리밋. 추가로 계정당 일일 사료 획득 상한, 기부 전환 구간 사후 정산 홀드(24h).
- **다계정·봇**: 가입 시 소셜 인증 1개 필수, 디바이스 핑거프린트, A7 이상탐지(플레이 리듬·이동 간격 분포). 랭킹 점수에 돌봄 성실도를 가중해 순수 퍼즐 매크로의 기대값을 깎는다.
- **개인 환금성 제로**: 사료 재화는 개인 소유가 아니라 공동 창고 적립 → 팔거나 옮길 수 없어 어뷰징 동기 자체가 작다.

### 7.2 학대 목적 입양 차단 (노출을 키우는 서비스의 책임)

- 성장치 LV.10 미달 시 신청 버튼 비활성 (시간·정성이 곧 1차 필터).
- 동일인 다계정·단기 다건 신청 패턴 탐지.
- A6 스크리닝: 신청서·상담 텍스트의 위험 신호(재판매 암시, 실외 단독 사육, 다두 신청 이력 등)를 근거와 함께 리포트로 보호소 전달.
- **시스템은 절대 자동 승인하지 않는다.** 자동으로 하는 건 차단과 경고뿐.

### 7.3 데이터 보호

| 데이터 | 민감도 | 처리 |
|---|---|---|
| 입양 신청서 (주소·주거·근무) | 높음 | 보호소 담당자만 열람(RLS), 저장 암호화, 종결 후 90일 파기 |
| 보호소 위치 | 동물 안전 직결 | 공개 화면은 시·군·구까지만 |
| 플레이 로그 | 매칭·어뷰징 근거 | 계정 단위 격리, 목적 외 사용 금지 |
| 미성년 이용자 | 결제·개인정보 | 결제 없음 기본, 입양 신청은 성인 인증 필수 |
| LLM 전송 | — | 신청서 개인 식별 정보 마스킹 후 전송 |

---

## 8. BM

| 순위 | 모델 | 구조 |
|---|---|---|
| 주력 | **도네이션 커머스** | 게임 내 사료/용품 = 실물 브랜드 제품. 공동 창고 목표 도달 → 브랜드가 실물 기부(CSR 예산). 브랜드는 게임 내 노출 + 기부 증서 + 일기 콘텐츠 태그를 얻는다 |
| 확장 | **입양 후 케어 구독** | 입양 성사 시 그 개의 성격 데이터를 이어받는 초보 반려인 AI 코치 (월 구독) |
| 로드맵 | B2G(지자체 입양률 KPI) · 기업 ESG 캠페인 · 캐릭터 굿즈 · IP 라이선싱 | — |

**광고 리워드(애드센스류)는 쓰지 않는다.** 기부 재원이 광고 단가에 종속되면 구조가 무너지고, 어뷰징 유인만 커진다.

---

## 9. 기술 아키텍처

```
┌─ Unity 6 WebGL ────────────────┐      ┌─ Supabase ──────────────────────┐
│ 홈(마당)·3매치·프로필·랭킹 UI    │      │ Auth (소셜 로그인)               │
│ 스프라이트 조합 렌더(A1 파라미터) │ HTTPS│ Postgres + RLS                  │
│ 입력 이벤트 큐 → 서버 전송       ├─────►│ Edge Functions (Deno/TS)        │
│ 로컬 상태 = 표시용 캐시만        │      │  ├ game-api (하트·돌봄·성장치)   │
└────────────────────────────────┘      │  ├ match3-verify (리플레이 검증) │
                                        │  ├ ai-pipeline (A1~A7, Claude)  │
┌─ 보호소 어드민 (Next.js 웹) ────┐      │  ├ donation-ledger (기부 정산)  │
│ 개 등록 · 리스크 리포트 열람      ├─────►│  └ public-data-sync (공공 API)  │
└────────────────────────────────┘      │ Storage (원본 사진·증서)         │
                                        │ Realtime (창고 게이지·랭킹)      │
                                        └─────────────────────────────────┘
```

**결정 사항 / 근거**

- **Unity WebGL**: 웹 필수 요건 충족. 모바일 브라우저 세로형 기준(393×852). 빌드 크기 목표 < 30MB(Brotli) — 도트 에셋이라 달성 가능.
- **Backend = Supabase로 확정 제안** (미정 항목에 대한 제안): Auth·RLS·Realtime·Edge Functions가 전부 있고 팀이 실서비스 경험 보유. 해커톤 2일 일정에서 대안(자체 서버) 검토는 리스크만 추가.
- **Claude API는 Edge Function에서만 호출** — 키가 클라에 나가지 않는다.
- **Realtime 채널**: 공동 창고 게이지·랭킹은 Supabase Realtime 구독 → "다 같이 채운다"는 감각의 핵심.

### 9.1 데이터 모델 (핵심 테이블)

```
users            (id, nickname, region, auth_provider, is_adult_verified, created_at)
dogs             (id, shelter_id, public_notice_id, name, breed, color, weight,
                  d_plus_start, photo_url, staff_memo, status[보호중|입양완료|임시보호],
                  region_sido, region_sigungu)
dog_characters   (dog_id FK, sprite_params jsonb, personality jsonb, ai_rationale)
yards            (id, user_id FK, dog_id FK, level, walk_stat, care_stat, bond_stat,
                  tiles jsonb, started_at)
care_actions     (id, yard_id FK, type[feed|walk|play], created_at)   -- 하트 차감은 서버
hearts           (user_id FK, count, last_refill_at)
match3_sessions  (id, user_id FK, board_seed, move_log jsonb, server_score,
                  feed_earned, verified boolean, created_at)
feed_warehouse   (id, season_id, current_amount, goal_amount, sponsor_id)
donations        (id, warehouse_id FK, sponsor_id, shelter_id, amount_kg,
                  certificate_holder_user_id,          -- 랭킹 1위
                  certificate_conames jsonb,           -- 2~10위 연명
                  proof_photo_url, executed_at)
rankings         (season_id, user_id, puzzle_score, care_weight, final_score, rank)
diaries          (id, dog_id FK, date, body, card_image_url, source jsonb)
interests        (user_id, dog_id, created_at)                        -- 관심견 max 3
adoption_apps    (id, user_id, dog_id, form_encrypted, sim_result jsonb,
                  screening_report jsonb, status[제출|검토중|승인|반려], created_at)
abuse_flags      (user_id, type, evidence jsonb, created_at)
shelters         (id, name, region, contact, admin_user_id)
```

### 9.2 API 표면 (Edge Functions, 전부 JWT 필수)

```
POST /game/care            {yard_id, type}                → 하트 차감, 스탯/성장치 갱신 (서버 계산)
GET  /game/yard            {}                             → 마당 상태 + 하트 + 성장치
POST /match3/start         {}                             → {session_id, board_seed}  (하트 차감)
POST /match3/submit        {session_id, move_log}         → 서버 리플레이 검증 → {score, feed_earned}
GET  /warehouse/status     {}                             → 공동 게이지 (Realtime 구독 병행)
GET  /rankings/current     {}                             → 주간 랭킹
GET  /dogs                 {sido, sigungu, sort=d_plus}   → 지역별 유기견
GET  /dogs/:id             {}                             → 프로필 + 성격 + 일기
POST /dogs/:id/interest    {}                             → 관심 등록 (max 3)
POST /adoption/simulate    {dog_id, env_form}             → A5 현실 시뮬레이션
POST /adoption/apply       {dog_id, form}                 → 자격 검증 → A6 스크리닝 → 보호소 큐
POST /shelter/dogs         {photo, memo}                  → A1+A2 파이프라인 → 캐릭터·스탯 생성
GET  /shelter/reports      {}                             → 신청자 리스크 리포트 (보호소 권한)
CRON daily-diary           —                              → A3 일기 배치 생성
CRON public-data-sync      —                              → 국가동물보호정보시스템 API 동기화
CRON abuse-scan            —                              → A7 이상탐지 배치
```

---

## 10. 역할별 작업 분해

### 기획자
- [ ] **P0** 성격 스탯 정답셋 20건 구축 (실제 공고 메모 → 기대 스탯) — A2 검증 기준
- [ ] **P0** 3매치 밸런스 시트 (스테이지 이동 수·사료 보상표·하트 회복 주기)
- [ ] **P0** 기부 전환표 (사료 재화 N = 실물 kg, 시즌 목표치) + 증서 문안
- [ ] **P0** 데모 시나리오 90초 대본 (실제 공고 1건 선정 포함)
- [ ] **P1** 성장치 레벨 곡선 (LV.1~10, 입양 자격 기준 근거 문서화)
- [ ] **P1** 일기 프롬프트 톤&매너 가이드 (의인화 수위 — 1인칭 사용하되 과장 금지)
- [ ] **P2** 뱃지 목록, 졸업 앨범 연출 정의

### 백엔드 개발자
- [ ] **P0** Supabase 스키마 + RLS 정책 (§9.1) — 특히 adoption_apps 암호화·shelter 권한 격리
- [ ] **P0** `match3-verify` 리플레이 검증 (보드 시드 결정론 보장 — 시드 RNG를 클라와 동일 구현)
- [ ] **P0** `game/care` 서버 권위 성장치 계산 + 하트 리밋
- [ ] **P0** `ai-pipeline` A1·A2 (등록 → 캐릭터 파라미터 + 성격 스탯)
- [ ] **P1** A3 일기 CRON, A5 시뮬레이션, A6 스크리닝
- [ ] **P1** `donation-ledger` + 랭킹 마감 배치 (1위 명의 확정 로직)
- [ ] **P1** 국가동물보호정보시스템 공공 API 동기화 (지역 필터·중복 제거)
- [ ] **P2** A7 어뷰징 배치, 디바이스 핑거프린트

### Unity 개발자
- [ ] **P0** WebGL 빌드 파이프라인 (모바일 세로, Brotli, 로딩 < 5s 목표) — **Day1 오전 최우선, 여기서 막히면 전체가 막힘**
- [ ] **P0** 홈 마당 씬 — Figma 아이소메트릭 도트 이식 (7×7 타일, 대표견, 성장치 패널, HUD, 4탭 네비)
- [ ] **P0** 스프라이트 조합 시스템 (A1 파라미터 → 팔레트 스왑 + 파츠 조합으로 개 캐릭터 렌더)
- [ ] **P0** 3매치 코어 (8×8, 5타일, 시드 기반 보드 생성 — 서버와 동일 RNG, move_log 기록·전송)
- [ ] **P1** 지역 목록·프로필·일기 카드 UI, 창고 게이지 Realtime 연동
- [ ] **P1** 대표견 성격별 행동 (경계심 높음 = 접근 시 회피 이동)
- [ ] **P2** 연출 (기부 달성 팡파레, 졸업 연출, 하트 파티클)

---

## 11. 마일스톤

### 해커톤 (2일)

| | 오전 | 오후 | 밤 |
|---|---|---|---|
| **Day 1** | WebGL 빌드 검증(Unity) / 스키마+RLS(BE) / 정답셋 착수(기획) | 홈 마당 씬 / A1·A2 파이프라인 / 밸런스 시트 | 3매치 코어 / care·match3 API 연결 |
| **Day 2** | 3매치 검증 연동 / 일기·매칭·스크리닝(BE) / 데모 대본 | 창고·랭킹 UI / 통합 테스트 | 리허설 ×3, 폴리싱 |

**해커톤에서 만들지 않는 것 (명시)**: 마당 확장 상점, 미니게임 2·3종, 실결제, 실제 기부 정산(모의 데이터로 시연), 보호소 어드민은 최소 폼만, 졸업 연출.

### 이후 로드맵
- **시즌 0 (1개월)**: 제휴 보호소 1곳 + 브랜드 1곳 파일럿, 실물 기부 1회 완주
- **시즌 1 (3개월)**: 지역 3곳 확장, 입양 매칭 첫 성사, 케어 구독 베타
- **시즌 2**: B2G 제안(지자체 입양률 KPI), 미니게임 확장, IP 상품화 검토

---

## 12. 리스크 & 미결정

| 항목 | 리스크 | 대응 / 결정 필요 |
|---|---|---|
| WebGL 모바일 성능 | iOS Safari 메모리 제한, 로딩 이탈 | 도트 에셋 경량화, 텍스처 아틀라스 1장, Day1 오전 실기기 검증 |
| 공공 API 품질 | 사진 화질·메모 부실 | A1 실패 시 기본 파츠 폴백, 제휴 보호소 직접 등록 병행 |
| 기부 이행 주체 | 브랜드 미확보 시 약속 불이행 | 파일럿은 목표액 소액 설정 + 팀 자체 이행, 확보 전까지 "모의 기부" 라벨 정직하게 표기 |
| 의인화 수위 | 일기가 개의 상태를 왜곡 | 기획 톤 가이드 + 보호소 근황 사실 기반 원칙, 과장 표현 금지 프롬프트 |
| 랭킹 1위 명의 | 부적절 닉네임이 증서에 인쇄 | 닉네임 필터 + 증서 발급 전 운영 확인 1단계 |
| **[미결정]** 하트 회복 주기 | 30분/개 가안 | 기획 밸런스 시트에서 확정 |
| **[미결정]** 서비스명 | "D+" 가칭 | 팀 결정 |
| **[미결정]** 백엔드 확정 | Supabase 제안 상태 | **이 PRD 리뷰에서 확정 요망** |

---

## 부록 A — 용어

| 용어 | 정의 |
|---|---|
| D+ | 보호소에서 기다린 날수. 프로필·홈에 상시 노출, 목록 기본 정렬 기준 |
| 성장치 | 마당 레벨(산책·돌봄·교감 종합). LV.10 = 입양 신청 자격 |
| 하트 | 행동력. 돌봄·미니게임 공용, 최대 5 |
| 사료 재화 | 3매치 보상. 개인 소유 불가, 전국 공동 창고 적립 |
| 공동 창고 | 전 유저 사료 재화 누적 게이지. 목표 도달 시 실물 기부 트리거 |
| 졸업 | 입양 성사. 실패·이탈 어휘 사용 금지 |
