# video/ — 데모 영상 자리

`video/demo.mp4` 를 넣으면 09번 슬라이드("데모 영상")의 점선 자리가
**자동으로 재생 컨트롤이 붙은 플레이어로 바뀐다.** 파일이 없으면 자리표시가 남는다.

- 권장: 16:9 · H.264 mp4 (브라우저 호환)
- 무대 시연이 원본이고 영상은 백업 — 4분 30초 시나리오는 10번 슬라이드 참조
- 다른 파일명을 쓰려면 `index.html`의 `<div class="video-slot" id="demoVideo" data-src="...">` 를 바꾼다
