# mini-game-1 PRD — MG1 · 3매치 퍼즐 (애니팡형)

**D+ (디플러스) 미니게임 1번 — 재화 루프의 P0 데모 대상**

| 항목 | 내용 |
|---|---|
| 문서 버전 | v1.1 (2026-07-22 · 이동 제한·수집 목표 전환, 뼈다귀 재화 확정, 결과 화면 확정) |
| 상위 문서 | [PRD.md](./PRD.md) §4.2(재화 루프) · §5(재화 체계) · §6.2(판매 연동 기부) · §7(커머스) |
| 상태 | **구현 완료** (입장·플레이·결과 전 사이클) — WebGL 빌드 검증만 남음 |
| 구현 범위 | **Unity 씬 `Assets/Scenes/mini-game-1.unity` 안에서만 작업한다.** 다른 씬·전역 설정 수정 금지 |
| 플랫폼 | Unity 6 WebGL · 모바일 세로 393×852 기준 |
| 장르 레퍼런스 | 애니팡 계열 3매치 (이동 제한 + 목표 수집형) |

> **용어 원칙 (상위 PRD 부록 A 계승)**: "유기견" 금칙 — **"보호견"으로 통일.** 게임 내 텍스트·연출·코드 주석까지 전부 해당.

---

## 1. 한 줄 정의

> 애견용품 블록을 3개 이상 맞춰 터뜨리는 **이동 20회 제한 3매치 퍼즐.**
> 목표는 **뼈다귀 블록 20개 모으기**이고, 모은 뼈다귀가 그대로 보상이 된다.
> 플레이 보상은 **뼈다귀**(기본 재화), 판 중간중간 등장하는 **브랜드 특수 블록**이
> 실제 애견용품 브랜드를 시각적으로 노출한다.

### 1.1 이 게임이 제품에서 하는 일

- **재화 루프의 입구** (§4.2): 발바닥 1개 소모 → 플레이 → 뼈다귀 획득. 뼈다귀는 사료 기부·지정 후원·마당 확장의 원천이 된다.
- **브랜드 노출 지점** (§7.1): 브랜드 특수 블록이 판 위에 등장해 "게임 참여 → 브랜드 인지"를 만든다. **구매 유도는 하지 않는다** — 자사몰 진입은 상점 탭의 몫.
- **해커톤 데모의 한 장면** (§2): 무대 4분 30초 시연 중 "3매치 플레이 → 보상 획득" 구간을 담당한다. 20회 풀 플레이가 아니라 **이동 10회로 줄여 보여줄 수 있어야 한다** (데모 모드, §8).

### 1.2 설계 가드 (상위 §1.2 원칙 적용)

| 상위 원칙 | 이 게임에서의 적용 |
|---|---|
| 원칙 2 — 죽음·안락사 금지 | 이동 소진 = "결과 집계"이지 실패·위험 연출이 아니다. 개가 굶거나 위험해지는 연출, 실패 시 슬퍼하는 캐릭터견 금지 |
| 원칙 3 — 기부 폐루프 | 결과 화면에서 캐릭터견이 "이 뼈다귀로 보호소 친구들 사료를 채워줄 수 있대요"라고 한 줄로 잇는다 (CTA 아님) |
| 어뷰징 방어 (§5.5) | 점수·보상 확정은 서버 권위가 원칙. 해커톤 범위의 타협은 §7 참조 |

---

## 2. 코어 게임플레이

### 2.1 기본 규칙 (애니팡 표준)

| 항목 | 스펙 | 비고 |
|---|---|---|
| 보드 | **7 × 7** 고정 그리드 | 393×852 세로 화면에서 여백 확보 가능한 크기. 레퍼런스 이미지 반영 시 재확정 |
| 조작 | 인접 블록 스와이프 교환 | 매치가 안 되는 교환은 되돌아감 (원위치 애니메이션) |
| 매치 | 가로·세로 3개 이상 동일 블록 | 대각선 없음 |
| 제한 | **이동 20회** (데모 10회) | 시간 제한 없음 — 타이머는 압박 장치라 쓰지 않는다 |
| 목표 | **뼈다귀 블록 20개 수집** | 달성 즉시 종료(클리어), 이동 소진 시에도 결과 집계 |
| 연쇄 | 터진 자리에 위에서 낙하 → 자동 연쇄 매치 | 연쇄 배수 적용 (§2.4) |
| 셔플 | 가능한 이동이 없으면 자동 셔플 | 페널티 없음 |
| 힌트 | 4초 무입력 시 가능한 매치 1개 하이라이트 | |

### 2.2 블록 — 일반 6종 (애견용품 테마)

블록은 전부 **애견용품**이다. 강아지 얼굴이 아니라 용품을 쓰는 이유: ① 브랜드 특수 블록(§3)과 세계관이 이어지고 ② 재화 아이콘(발바닥·육포)과 시각적으로 겹치지 않는다.

| # | 블록 | 시각 가이드 (레퍼런스 반영 전 가안) |
|---|---|---|
| 1 | 사료 그릇 | 빨강 계열 |
| 2 | 뼈다귀 | 아이보리 계열 |
| 3 | 테니스공 | 노랑-초록 계열 |
| 4 | 간식 (개껌) | 갈색 계열 |
| 5 | 리드줄 | 파랑 계열 |
| 6 | 강아지 장난감 (삑삑이) | 분홍 계열 |

- **금지**: 육포 모양 블록 (과금 재화와 혼동), 발바닥 모양 블록 (행동력과 혼동).
- 색맹 대응: 색상 + 실루엣이 모두 구분되도록 한다.

### 2.3 특수 블록 (매치 크기 보상)

| 생성 조건 | 특수 블록 | 효과 |
|---|---|---|
| 4개 일렬 | **로켓 장난감** | 가로 또는 세로 한 줄 제거 (생성 방향 기준) |
| 5개 L/T자 | **폭죽 공** | 주변 3×3 제거 |
| 5개 일렬 | **매직 사료** | 같은 종류 블록 전체 제거 (아무 블록과 교환 시 발동) |

특수 블록끼리의 조합(로켓+로켓 등)은 **v0.2 이후 검토** — 데모 필수 아님.

### 2.4 점수

```
기본: 블록 1개당 10점
연쇄: 콤보 n단계 → ×(1 + 0.5n)  (1연쇄 ×1.5, 2연쇄 ×2.0 …)
특수 블록 발동으로 제거된 블록: 개당 15점
피버: 게이지 만충 시 10초간 전 점수 ×2 + 블록 낙하 속도 상승
```

- 피버 게이지는 매치할 때마다 차오르고 무입력 시 서서히 감소 (애니팡 표준).
- 점수는 재화가 아니라 주간 랭킹용이다. 보상 산정은 §4.

---

## 3. 브랜드 통합 — "중간중간 브랜드가 보인다"

브랜드는 협업 대상이다(상위 §7). 이 게임에서 브랜드가 노출되는 지점은 **인게임 로고 블록 한 곳뿐이다.** 판 전체가 광고판이 되면 게임도 브랜드도 같이 죽는다.

### 3.1 노출 지점 ①: 브랜드 특수 블록 (인게임)

- 플레이 중 **8초마다 1개(데모 5초), 판 위 동시 최대 1개**로 브랜드 로고 블록이 드롭된다. 시작 후 약 3초면 첫 블록이 등장한다.
- **3사(바잇미·페스룸·하울팟) 중 매번 랜덤**으로 뽑는다.
- 인접 매치에 포함시켜 터뜨리면 **보너스 점수 300점 + 뼈다귀 5개**.
- 터뜨리지 못하고 시간이 끝나도 페널티 없음.
- **시즌 스폰서십 단위로 교체된다** (§7.1 "시즌당 1~2개 브랜드"). 블록 스프라이트·상품명은 데이터로 분리해 씬 수정 없이 갈아끼울 수 있게 한다.

### 3.2 쿠폰·수집 개념은 두지 않는다

- **브랜드 로고가 판 위에 보이는 것 자체가 홍보 효과**다. 쿠폰 수령·로고 수집 같은 장치를 얹지 않는다.
- 결과 화면에도 브랜드를 노출하지 않는다 — 보상 화면의 주인공은 보상이어야 하고, 플레이 직후 상점으로 보내면 홍보가 아니라 영업이 된다 (§9-7).

### 3.4 시즌 1 후보 브랜드 (리서치 완료 · 2026-07-21)

국내 강아지 용품 브랜드 3곳을 후보로 선정하고 로고 에셋을 확보했다. **아직 협업 계약이 없으므로 게임 내 노출 시 반드시 "모의 협업" 라벨을 붙인다** (§3.5).

| 브랜드 | 분야 | 공식 사이트 | 확보 에셋 |
|---|---|---|---|
| **바잇미 (BITE ME)** | 강아지 장난감·의류·간식 (산리오 콜라보 등) | biteme.co.kr | 로고 PNG(282×42) + SVG 래퍼 |
| **페스룸 (PETHROOM)** | 케어·위생용품, IoT 급식기 (수의사 공동개발) | pethroom.com | 로고 PNG(409×48) + SVG 래퍼 |
| **하울팟 (HOWLPOT)** | 산책용품·포터블매트·하우스 | howlpot.com | 로고 PNG 다크/라이트(381×90 · 369×87) + SVG 래퍼 |

- 에셋 위치: `Assets/UI/MiniGame1/Brands/` (mini-game-1 전용 폴더).
- **세 브랜드 모두 공식 SVG를 공개하지 않는다** — 공식 사이트에서 받은 PNG 원본 + PNG를 임베드한 SVG 래퍼로 보관한다. Unity 스프라이트로는 PNG를 쓴다 (Unity는 SVG를 기본 지원하지 않음).
- 브랜드별 게임 내 매핑 가안: 바잇미 → 장난감 블록 계열 특수 블록, 페스룸 → 케어용품 블록, 하울팟 → 산책용품 블록. 시즌당 1~2개만 활성화한다 (§7.1).

### 3.5 브랜드 통합 금지 사항

- 로딩 화면·보드 배경·블록 전면을 브랜드 로고로 채우지 않는다.
- 브랜드 블록이 게임 밸런스를 바꾸지 않는다 (점수 보너스만, 판도를 뒤집는 효과 금지).
- 실존 브랜드 확보 전까지는 **가상 브랜드 이름 + "모의 협업" 라벨**을 노출한다 (§6.5 정직성 규칙 계승).

---

## 4. 재화 연동 — 뼈다귀가 기본 재화다

**이 게임에 "포인트"는 없다.** 유저가 다루는 재화는 **발바닥(행동력)** 과 **뼈다귀(기본 재화)** 둘뿐이다.

> **상위 PRD와의 차이**: 상위 PRD §5.1은 기본 재화를 "포인트"로 부른다. MG1은 이를 **"뼈다귀"로 부른다.** 유저에게 추상적인 숫자(P)를 주는 대신, *판에서 직접 모은 뼈다귀 블록이 그대로 재화가 되는* 구조가 훨씬 이해하기 쉽기 때문이다. → 상위 PRD §5.1의 명칭 통일은 팀 합의 필요 (§9-6).

### 4.1 보상 규칙 (확정)

| 항목 | 규칙 |
|---|---|
| 입장 | **발바닥 1개** 소모. 0개면 입장 불가 |
| 기본 보상 | **모은 뼈다귀 블록 1개 = 뼈다귀 1개** |
| 목표 달성 보너스 | 목표(20개) 달성 시 **×2** |
| 브랜드 블록 보너스 | 브랜드 블록 1개당 **+5 뼈다귀** |
| 일일 상한 | **200 뼈다귀** (발바닥 5회 × 40) |
| 점수 | **재화가 아니다.** 주간 랭킹 산정에만 쓴다 (§5.3) |

```
한 판 최대 = 20블록 × 2(달성) = 40 뼈다귀  (+ 브랜드 블록 보너스)
하루 최대 = 40 × 발바닥 5회 = 200 뼈다귀  → 일일 상한과 일치
```

- **왜 점수를 재화에서 분리했나**: `floor(점수/100)` 같은 환산은 유저가 "왜 이만큼 받았는지" 알 수 없다. 모은 뼈다귀가 곧 보상이면 결과 화면을 보지 않아도 플레이 중에 이미 보상을 예측할 수 있다. 점수는 랭킹이라는 별도 역할을 갖는다.
- 랭킹 반영 시 이 게임의 뼈다귀는 `origin=play`로 태깅된다 (§5.5).
- **뼈다귀로 발바닥을 살 수 없다** (상위 §5.2) — 게임 내 어디에도 그 동선을 만들지 않는다.

---

## 5. 화면 흐름 (씬 내부 3개 상태)

`mini-game-1.unity` 씬 하나 안에서 상태 전환으로 처리한다. 별도 씬 추가 금지.

```
[A. 입장 확인]              [B. 플레이]                  [C. 결과]
발바닥 1 소모 안내      헤더(이동·점수) · 피버 바     메달 팝인 → 뼈다귀 카운트업
[시작하기]          →   7×7 보드 · 브랜드 블록    →   → 축하 불꽃
                        하단 목표 바(뼈다귀 n/20)     캐릭터견 코멘트 한 줄
                        코치 말풍선(조건부)           [한 번 더] [홈으로]
```

- **A. 입장 확인**: 발바닥 잔량 표시, 소모 안내. 부족 시 시작 버튼 비활성.
- **B. 플레이**: 상단 헤더에 이동·점수, 그 아래 피버 게이지(피버 중 노랑→빨강). 목표 진행은 보드 아래 한 줄.
- **C. 결과**: 보상이 주인공이다. 메달 팝인 → 획득 뼈다귀 카운트업 → 불꽃 순으로 재생한다. 낮은 점수여도 실패 연출 없음.
- **CTA는 `한 번 더` / `홈으로` 2개뿐**이다 (§9-7).
- 마당(홈)과의 진입·복귀 연결은 홈 씬 담당자와 인터페이스만 합의하고, **이 씬에서는 진입점 함수만 노출한다**.

---

## 6. 기술 명세 (구현 범위)

### 6.1 씬·폴더 규칙

- 씬: `Assets/Scenes/mini-game-1.unity` — **이 씬만 수정**.
- 스크립트: `Assets/Scripts/MiniGame1/` 아래에만 추가 (신규 폴더).
- 스프라이트·프리팹 등 전용 에셋: `Assets/UI/MiniGame1/` (신규 폴더).
- 공용 에셋(Modern UI Pack 등)은 **참조만** 하고 원본 수정 금지.

### 6.2 컴포넌트 구조 (가안)

| 컴포넌트 | 책임 |
|---|---|
| `MG1GameManager` | 상태 머신 (입장→플레이→결과), 이동 수·목표 집계, 사운드, 데모 모드 스위치 |
| `BoardController` | 7×7 그리드 생성, 스와이프 입력, 매치 판정, 낙하·리필 |
| `MatchResolver` | 매치 탐색·연쇄 계산 (보드와 분리된 순수 로직 — 유닛 테스트 대상) |
| `BlockFactory` | 일반 6종 + 특수 3종 + 브랜드 블록 생성. 브랜드 데이터는 `ScriptableObject`로 분리 |
| `ScoreSystem` | 점수·콤보·피버 계산 (순수 로직) |
| `RewardClient` | 결과 → 뼈다귀 지급 요청. 데모에선 로컬 mock, 인터페이스는 Edge Function 규격 |

- **로직과 뷰 분리**: `MatchResolver`·`ScoreSystem`은 UnityEngine 의존 없는 순수 C#으로 작성해 테스트 가능하게 한다 (10-survey-engine의 "화면과 분리된 로직" 방침과 동일).

### 6.3 서버 권위 — 해커톤 범위의 타협

상위 §5.5 원칙은 "모든 재화 증감은 Edge Function"이다. 해커톤 데모에서는:

| 구간 | 데모 구현 | 정식 구현 |
|---|---|---|
| 발바닥 차감 | 로컬 상태 | Edge Function |
| 점수 계산 | 클라이언트 | 클라 계산 + 서버 검증(플레이 로그 리플레이 or 상한 검증) |
| 뼈다귀 지급 | 로컬 mock (인터페이스는 서버 규격) | Edge Function + ledger 기록 |

**타협을 숨기지 않는다**: 데모 빌드의 mock 구간은 코드에 `// DEMO-MOCK` 태그를 남기고, 06-handoffs 문서에 목록을 기록한다.

---

## 7. 완료 기준 (P0 데모)

1. 발바닥 1 소모 → 이동 20회 플레이 → 뼈다귀 지급까지 한 사이클이 끊김 없이 돈다.
2. 3매치·낙하·연쇄·특수 블록 3종이 오동작 없이 작동한다.
3. 브랜드 블록이 주기 드롭되고(3사 랜덤), 터뜨리면 보너스 뼈다귀가 지급된다.
4. 결과 화면이 메달 → 뼈다귀 카운트업 → 불꽃 순으로 재생되고, CTA는 2개뿐이다.
5. WebGL 빌드에서 393×852 세로 비율로 프레임 드랍 없이 구동된다 (모바일 브라우저 1종 이상 확인).
6. **데모 모드**: 이동을 10회로 줄이고 브랜드 블록 드롭을 앞당기는 스위치가 있다 — 무대 4분 30초 시연용 (§1.1).

## 8. 이 버전에서 제외 (결정 근거 보존)

| 항목 | 결정 | 이유 |
|---|---|---|
| 스테이지·레벨 시스템 | 제외 | 애니팡형 단판(이동 제한)으로 충분. 스테이지는 리텐션 검증 후 |
| 특수 블록 조합 (로켓+폭죽 등) | 보류 | 데모 필수 아님, v0.2 검토 |
| 아이템(시간 연장·셔플권 등) | 제외 | 재화 사용처 확장은 §5 밸런스 시트 확정 후 |
| 멀티플레이·친구 랭킹 | 제외 | 주간 랭킹(§5.3)은 서버 몫, 이 씬 범위 아님 |
| 사운드 커스텀 제작 | 축소 | 기존 에셋 활용, 폴리싱은 데모 이후 |

## 9. 확정 사항 (v1.0 가안 확정 — 변경 시 이 표를 갱신하고 사유를 남긴다)

| # | 항목 | 확정 내용 | 근거 |
|---|---|---|---|
| 1 | 보드 크기 | **7×7** | 393px 폭에서 블록 1개 ≈ 48px — 터치 최소 권장(44px) 충족. 레퍼런스 이미지가 다른 그리드를 쓰면 부록 R에서 재검토 |
| 2 | 블록 아트 스타일 | **플랫 2D** (마당의 아이소메트릭 도트와 분리) | 3매치는 0.3초 안에 블록 종류가 식별돼야 한다. 아이소메트릭은 실루엣 구분을 해친다. 홈↔게임 전환 연출로 톤 차이를 흡수 |
| 3 | 보상 재화 | **뼈다귀** — 모은 블록 1개 = 1개, 달성 시 ×2, 일일 상한 200 (§4.1) | 점수 환산은 유저가 근거를 알 수 없다. 모은 것이 곧 보상이어야 한다 |
| 4 | 시즌 1 활성 브랜드 | **바잇미 1곳** (모의 협업 라벨) · 대표 상품: 강아지 장난감 | 블록 테마(장난감·삑삑이)와 카테고리가 가장 겹쳐 특수 블록의 이질감이 적다. 페스룸·하울팟은 시즌 2 후보로 보존 (§3.4) |
| 5 | 피버 연출 | 게이지 바 색 전환(노랑→빨강)만. **파티클은 매치 폭발 1종만 사용** | WebGL 모바일 브라우저 성능 예산. 별도 뱃지는 화면만 어지럽힘 |
| 6 | 재화 명칭 | MG1은 **뼈다귀**로 부른다 (§4) | 상위 PRD §5.1의 "포인트"와 명칭이 다르다 — **전 서비스 통일 여부는 팀 결정 필요** |
| 7 | 결과 화면 CTA | **`한 번 더` / `홈으로` 2개만** | 미니게임은 재화 루프(§4.2)까지만 책임진다. 후원·커머스 연결은 홈·추천 탭의 몫 (상위 §4.4 "다음 단계 하나만") |

## 10. 레퍼런스 이미지 반영 절차

이미지 레퍼런스가 도착하면: ① 부록 R 표에 "가져올 것 / 버릴 것"을 기록하고 ② §9 확정 사항과 충돌하는 항목만 사유와 함께 갱신한다. **이미지가 늦어져도 구현은 §9 가안으로 진행한다** — 아트 스왑이 가능하도록 블록 스프라이트는 전부 `BlockFactory`의 데이터(ScriptableObject) 참조로만 연결한다 (§6.2).

---

## 부록 R — 레퍼런스 이미지

*(수령 대기. 이미지 도착 시 항목별로 "무엇을 가져오고 무엇을 버리는지"를 기록한다 — 그리드 크기, 블록 스타일, 피버 연출, UI 배치, 색감.)*

| # | 이미지 | 가져올 것 | 버릴 것 |
|---|---|---|---|
| R1 | *(대기)* | | |
